# 新页面默认模板：基础背景

用户要求：此网站此后每次新建页面，默认复用“基础背景”，无需再次确认。

- 使用 `src/main.jsx` 中的 `PortfolioPage` 组件；该组件即“基础背景”模板。
- 默认继承顶部栏（CYANO、专业说明、居中年份、三个交互动效按钮）、当前 Waves 动态背景、底部信息（3D CONCEPT MODELING / 李帅奇作品集）。
- 顶部栏与底部信息在 App 中全局固定显示一次，新页面不重复创建。慢速滚轮每次手势切换一页；120ms 内标准化滚动速度达到 2px/ms 时允许连续翻页，绕过滚动锁定，保持整屏吸附。
- 新页面置于已有页面下方，使用唯一 id 和语义 label，至少占一整屏；中间内容区默认留空，按用户要求填入。
- 不默认复制首页的 CYANO 视频字形，不添加未要求的文案或功能。
- 保持现有响应式样式、字体、背景颜色及动效参数；用户明确提出的修改优先。

示例：`<PortfolioPage id="page-3" label="第三页" />`。

## 视频卡片默认行为

- 新增视频卡片时，使用 `ProjectCards.jsx` 的 `video` 字段和已有交互：仅鼠标悬停播放，移开暂停并回到第一帧；默认静音、循环、行内播放，不自动播放。
- 保留统一宽度、原始比例及现有黑色遮罩和点击跳转。

## 卡片悬停文字默认样式


- 后续添加卡片文字时，统一复用第一张卡片的 `.project-hover-label`：左侧 16px、底部 14px、字号 14px，沿用相同字体、颜色和淡入效果。
- 仅鼠标悬停时显示，移开隐藏；除非用户明确要求，不单独调整某张卡片的位置和字号。

## 作品集内部模板

- 用户将当前第一张卡片的详情页命名为“作品集内部模板”。后续制作其他卡片详情页时，默认复用 `src/ProjectStudy.jsx` 和 `src/ProjectStudy.css` 的结构与样式，不重新设计。
- 继承 `PortfolioPage` 的背景和固定顶部、底部信息。左上角顶部栏下方固定竖排“返回项目列表”。
- 左侧标题与小字项目介绍，右侧使用软件图标；下方大图区域支持替换为图片或视频，再下方为 3×3、16:9 九宫格。
- 九宫格悬停轻微放大，点击以动画打开完整全屏预览，左上角返回按钮或 Esc 关闭。详情内容独立纵向滚动。
- 新卡片只替换项目标题、介绍、软件和媒体素材，沿用模板布局与交互。当前只应用第一张卡片，不自动批量修改其他卡片。
